## login(user_id, password)

   Logs the user in using their user_id and password. Returns `True`
   if login succeeds, `False` otherwise.

## logout(session_token)

   Logs out a user given their session_token.

## reset_password(email, temp_password)

   Resets a user's password given their email and temp_password.